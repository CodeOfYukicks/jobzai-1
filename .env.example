# Environment Variables Example

# OpenAI API Key (Required for GPT-4o and GPT-4o Vision)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (Optional, for Claude if used)
# Get your key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Node Environment (development or production)
NODE_ENV=development

# Firebase Configuration (Optional, if using Firebase)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Instructions:
# 1. Copy this file to .env (without the .example extension)
# 2. Replace the placeholder values with your actual API keys
# 3. Never commit the .env file to version control (it's in .gitignore)
